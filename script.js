// let bar = document.querySelectorAll(".skill-bar");
// bar.forEach((bar) => {
//   const percent = bar.querySelector(".percent").innerText.trim();
//   const progress = bar.querySelector(".skill-progress");
//   progress.style.width = percent;
// });

// window.addEventListener("DOMContentLoaded", () => {
//   document.querySelectorAll(".skill-bar").forEach((bar) => {
//     const percentText = bar.querySelector(".percent").innerText.trim();
//     const numeric = parseFloat(percentText); // strips the “%”
//     bar.querySelector(".skill-progress").style.width = numeric + "%";
//   });
// });
